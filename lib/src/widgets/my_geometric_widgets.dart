import 'package:flutter/material.dart';

class MyGeometricWidget{

  EdgeInsets setPadding(double value){
    var padding = EdgeInsets.all(value);
    return padding;
  }

   EdgeInsets setMargin(double value){
     var margin = EdgeInsets.all(value);
     return margin;

  }

}